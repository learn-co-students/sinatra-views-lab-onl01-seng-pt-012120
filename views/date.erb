
    <!DOCTYPE html>
<html>
  <body>
    <h1>Today</h1>
    <% time = Time.new %>
    <% time.strftime("The date is %A, %B %d, %Y") %>
  </body>
</html>


<% time = Time.new %>
<% time.strftime("The date is %A, %B %d, %Y")
